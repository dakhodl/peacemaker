#!/bin/sh

# warning - this will delete other containers.
# I have a sandbox where this is all that's here.
# and I need this script to fine tune the onboarding experience


docker container prune
docker volume prune
docker image prune --all